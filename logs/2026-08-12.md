# 2026-08-12 — 회차 14

## 목표 지표
- 지갑 연동: 아니오 (Blink API 키 형식 오류, HTTP 401) · 잔액: 조회 불가 · 수령액(baseline 제외): 0 / 600 sats

## 이번 회차 완료
- (A3) testnet4 faucet(coinfaucet.eu) 사용 시 잔액 확인이 막히지 않도록, mempool.space의 testnet4 Esplora API(`https://mempool.space/testnet4/api`)가 실제로 응답함(블록 높이 조회 성공)을 확인. `harness/ACTION_REQUIRED.md` A3 섹션의 모호했던 `{ baseUrl: '...' }` 자리표시자를 이 실제 URL과 예시 명령으로 교체. `src/balance.js`는 이미 `baseUrl` 옵션을 지원하므로 코드 변경은 불필요.
- `npm run check-goal`(401)·`npm run verify-blink-key`(`ak_v2_` 접두사)로 B1/B2 상태를 재확인 — 07-17부터 10회차 연속 미교체. 반복 진단 지양 방침에 따라 별도 재조사 없이 확인만 하고 종료.
- `npm test` → 59 passed (문서만 변경했으므로 회귀 확인 목적).

## 사람이 할 일 (블로킹)
- (B1) dashboard.blink.sv에서 Read+Receive 스코프의 `blink_` 접두사 API 키를 재발급해 `.env`의 `BLINK_API_KEY`를 교체. 현재 값은 `ak_v2_` 접두사(다른 서비스 키로 추정), 07-17부터 10회차 연속 미교체. 정확한 경로는 `harness/ACTION_REQUIRED.md` B1 참고.
- (A3) testnet/testnet4 faucet에서 소액 테스트 코인 수령 (`harness/ACTION_REQUIRED.md` A3 — 니모닉·주소 생성 명령, mempool.space faucet 및 백업 coinfaucet.eu 경로, 잔액 확인 명령까지 갱신됨).
- (C3) Stacker News 계정 개설 — B1을 기다릴 필요 없이 Nostr 로그인으로 지금 바로 가능 (`harness/ACTION_REQUIRED.md` C3).

## 다음 회차 계획
- B1/B2(.env 키 접두사)가 여전히 `ak_v2_`라면 재진단 없이 확인만 하고, task:null 상태가 유지되는 한 남은 human-blocked 작업(B1/A3/C3) 중 아직 다루지 않은 구체적 준비물이 있는지부터 검토. 없으면 로그만 기록.
- 사람이 B1을 완료해 `.env` 키가 `blink_` 접두사로 바뀌면, 다음 회차에서 `npm run check-goal`의 `configured:true` 확인 → B2 done 처리 → B3(자가 인보이스 결제 실검증)로 진행.
