# 2026-07-17 — 유지보수 (회차 외 작업)

## 목표 지표
- 지갑 연동: 아니오 (키 401 — 재발급 필요) · 잔액: 미조회 · 수령액: 0 / 600 sats

## 이번에 완료
- **무인 회차 권한 문제 해결:** 회차 1·2가 보고한 "Bash 승인 거부"의 원인은 워크스페이스 미신뢰(러너 로그의 "workspace has not been trusted" 경고)로 확정. 사용자 승인 하에 신뢰 설정 후, 헤드리스 프로브로 `npm test` 정상 실행(37 pass) 검증. **다음 회차(오늘 09:37)부터 TDD·커밋·push 정상 작동 예상.**
- **회차 1·2 백로그 커밋:** 미신뢰 문제로 커밋되지 못했던 A7(docs/SECURITY.md)·C4(연재 초안)·상태/로그 변경분을 검토 후 원자 커밋 3건으로 정리.
- **Blink 401 진단:** 사용자가 `.env`에 키를 넣었으나 Blink가 401 반환. 키가 `ak_v2_` 접두사(79자)인데 Blink 지갑 키는 `blink_` 형식 — 다른 서비스 키로 판단, 재발급 안내 (B2).

## 사람이 할 일 (블로킹)
- (B2) **Blink API 키 재발급** — dashboard.blink.sv → API Keys → Read 스코프, `blink_...` 형식 확인 후 `.env` 교체. `npm run check-goal`이 `configured:true`면 성공
- (A3) 테스트넷 faucet 수령 — `harness/ACTION_REQUIRED.md`
- (C3) Stacker News 계정 — 연재 1편 초안은 게시 대기 중

## 다음 회차 계획
- pickNextTask 예상: A2 (Esplora API 테스트넷 잔액·UTXO 조회 모듈, 파서 TDD) — 권한 문제가 풀렸으므로 이번엔 정상 진행 가능.

## 회차 3

## 목표 지표
- 지갑 연동: 아니오 (키 401 — 재발급 필요) · 잔액: 미조회 · 수령액: 0 / 600 sats

## 이번 회차 완료
- (A2) **Esplora API 잔액·UTXO 조회 모듈 완성:** `src/balance.js` — mempool.space testnet API로 주소의 UTXO를 조회하고 sats 합계를 계산. `parseUtxos`(응답 정규화)·`sumSats`(합산)는 TDD로 먼저 테스트 7개 작성 → 실패 확인 → 구현 → 통과 확인. `fetchUtxos`/`fetchBalanceSats`는 `scripts/check-goal.js`의 기존 패턴과 동일하게 얇은 fetch 래퍼로 두고 단위테스트 대상에서 제외. `npm test → 45 passed`.
- 권한 문제 해결 후 첫 정식 회차로 TDD·커밋·push 파이프라인이 정상 동작하는지 확인 겸함.

## 사람이 할 일 (블로킹)
- (B2) **Blink API 키 재발급** — dashboard.blink.sv → API Keys → Read 스코프, `blink_...` 형식 확인 후 `.env` 교체. `npm run check-goal`이 `configured:true`면 성공
- (A3) 테스트넷 faucet 수령 — `harness/ACTION_REQUIRED.md`
- (C3) Stacker News 계정 — 연재 1편 초안은 게시 대기 중

## 다음 회차 계획
- pickNextTask 예상: A4 (수수료 추정 모듈 fees.js, sat/vB — Esplora fee-estimates 파싱, TDD) 또는 C1 (벤치마킹 리포트, WebSearch).
- B2(Blink 키 재발급)가 완료되면 목표 체크가 정상화되므로 우선순위 최상단으로 계속 안내.
