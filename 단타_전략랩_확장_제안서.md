# 단타 트레이딩 · 전략 랩 확장 제안서

**상위 문서:** [비트코인_획득_전략_및_개발제안서.md](비트코인_획득_전략_및_개발제안서.md) (원본 전략 — 판단이 애매할 때 이 문서보다 원본이 우선)
**작성일:** 2026-08-15 (회차 17 시점)
**목적:** 기존 "첫 600 sats 수령" 목표에 **단타 트레이딩 증식 + 전략 랩 SaaS**를 삼각 편대로 결합한다.

---

## 1. 왜 지금 확장하는가

원본 전략은 "상품을 만들어 팔아 첫 sats를 받는다"는 단선 구조다. 두 가지 문제가 관측됐다.

1. **B1(라이트닝 지갑 개설)이 29일째·13회차 연속 블로킹.** 목표 판정으로 가는 유일한 경로가 사람 작업 하나에 묶여 있어, 그동안 하네스는 `task:null`로 헛돌았다.
2. **수익 상한이 낮다.** 1,000 sats짜리 디지털 상품 한 종으로는 반복 매출이 생기지 않는다.

확장의 핵심은 **자본이 필요 없는 축을 먼저 돌려 B1 의존성에서 하네스를 풀어주는 것**이다. 백테스트·페이퍼 트레이딩은 지갑도 시드도 필요 없고, 거래소 공개 OHLCV는 무료다.

---

## 2. 순서의 논리 — 왜 "트레이딩 → SaaS"가 아니라 "랩 → 콘텐츠 → SaaS → 트레이딩"인가

직관적으로는 "단타로 벌어서 그걸 홍보하고, 겸사겸사 툴을 판다"는 순서다. 그러나 현재 보유 자산은 0이고, 검증된 전략도 아직 없다. 자본 0에서 성립하는 유일한 배치는 다음과 같다.

```
D. 전략 랩 (자본 0, 지금 즉시)
   └─ 백테스트 엔진 → 검증된 전략 + 성과 지표
        ├──→ C. 콘텐츠 (백테스트 결과 연재 = 홍보 = 무료 마케팅)
        ├──→ E. 페이퍼 트랙레코드 (공개 검증 → 신뢰도)
        │       └─ 30일 통과 시에만 ──→ 소액 실거래 착수 (사람 집행)
        └──→ F. 랩 SaaS (구독 = 반복 매출)
                └─ 결제는 B4(라이트닝 결제 페이지) 재사용
```

**핵심:** 트레이딩 수익이 SaaS를 먹여살리는 게 아니라, **랩이 만들어낸 검증 결과가 콘텐츠·SaaS·트레이딩 세 곳에 동시에 재사용된다.** 원본 전략의 "만드는 과정 = 마케팅 = 상품" 삼중 구조와 정확히 같은 논리이며, 대상만 지갑에서 전략 랩으로 바뀐 것이다.

시너지가 실제로 발생하는 지점:

| 산출물 | 트레이딩에서 | 홍보에서 | SaaS에서 |
|---|---|---|---|
| 백테스트 엔진 | 전략 검증 도구 | "직접 만든 엔진" 기술 콘텐츠 | 제품 그 자체 |
| 페이퍼 트랙레코드 | 실거래 착수 게이트 | 신뢰의 근거 | 전환율의 근거 |
| 라이트닝 결제(B4) | — | — | 구독 결제 인프라 |

---

## 3. 트랙 D — 전략 랩 (백테스트 엔진)

### 3-1. 데이터 소스: 바이낸스 (확정)

- `GET /api/v3/klines` — 인증 불필요, 무료, 과거 캔들 대량 수집 가능
- USDT 마켓 기준. 영문 콘텐츠·글로벌 SaaS 독자층과도 정합
- 실거래 전환 시에도 같은 심볼·같은 데이터로 백테스트↔실전 간극이 최소화됨

### 3-2. 백테스트 정직성 규약 (이 프로젝트의 핵심 자산)

성과를 **공개적으로 홍보**할 것이므로, 숫자를 부풀리는 흔한 함정을 엔진 레벨에서 구조적으로 차단한다. 이 규약이 곧 SaaS의 차별점이다.

1. **룩어헤드 편향 차단:** `i`번째 봉 **종가까지의 정보**로 판단한 목표 포지션은 **`i+1`번째 봉 시가**에 체결한다. 같은 봉의 종가로 체결하지 않는다.
2. **비용 반영 필수:** 수수료(taker, bps)와 슬리피지(bps)를 진입·청산 양쪽에 적용한다. 비용 0인 백테스트 결과는 공개하지 않는다.
3. **인샘플/아웃오브샘플 구분 표기:** 파라미터를 맞춘 구간과 그러지 않은 구간의 성과를 반드시 나눠서 보고한다.
4. **페이퍼/실거래 라벨:** 모든 공개 성과에 `paper` 또는 `live` 라벨을 붙인다. 섞어서 하나의 곡선으로 그리지 않는다.
5. **생존 편향 주의:** 상장폐지·심볼 변경 이력이 있는 종목을 임의로 제외하지 않는다.

### 3-3. 구현 범위

```
src/
├── klines.js       # 바이낸스 klines 수집·정규화 (파싱은 순수 함수, fetch는 얇은 래퍼)
├── indicators.js   # SMA / EMA / RSI / ATR
├── backtest.js     # 체결·비용·자산곡선·성과 지표
└── strategies/     # 전략 1종씩. (candles) => targetPositions[]
```

성과 지표: 총수익률, 최대낙폭(MDD), 승률, 손익비(profit factor), 트레이드 수.

### 3-4. 완료 기준

- 알려진 입력에 대해 손으로 계산한 자산곡선과 엔진 출력이 일치 (단위 테스트)
- 수수료·슬리피지를 켰을 때 성과가 반드시 나빠짐을 테스트로 고정
- 1호 전략의 인샘플/아웃오브샘플 성과가 리포트로 출력됨

---

## 4. 트랙 E — 페이퍼 트랙레코드 → 실거래

### 4-1. 실거래 착수 게이트 (명시적 조건)

소액 실거래 시드를 투입하기로 했으나, **투입 시점은 다음 조건을 모두 통과한 뒤로 못박는다.**

**2026-08-15 개정:** 판정 근거를 페이퍼 30일에서 **워크포워드 이어붙인 곡선**으로 바꿨다(사용자 결정). 페이퍼는 하루에 하루치만 쌓여 판정까지 한 달이 걸리는 반면, 워크포워드는 같은 규율(파라미터는 학습 구간에서만 선택)을 유지하면서 수년치 아웃오브샘플을 즉시 잰다. 페이퍼 아레나는 폐기하지 않고 보조 기록으로 유지한다.

- [ ] 폴드별 아웃오브샘플 구간을 **이어붙인 연속 곡선**에서 비용 반영 후 플러스 수익
  (폴드 수익률의 산술평균은 복리와 경계 낙폭을 놓치므로 판정 근거로 쓰지 않는다)
- [ ] 같은 구간 매수보유 초과
- [ ] 최대낙폭 ≤ 35%
- [ ] **워크포워드 효율(WFE) ≥ 0.2** — 학습 성과가 검증에서 최소한 일부라도 재현될 것
- [ ] 트레이드 ≥ 30 (표본)
- [ ] **심볼 과반**에서 위 조건 통과 — 한둘에서의 통과는 재현에 실패한다(`docs/research-campaign.md` §2)
- [ ] 1회 손실 한도·일일 손실 한도가 전략에 코드로 박혀 있음

판정 구현은 `src/research.js`의 `evaluateWfGate`가 독점한다. 기준을 바꾸려면 그 파일과 이 문서를 함께 고쳐야 한다.

게이트를 통과하기 전에 시드를 넣는 것은 이 문서 기준으로 **계획 위반**이다. 조급해질 때 돌아와서 읽으라고 적어 둔다.

### 4-2. 실주문 집행 원칙 (절대 규칙)

- **에이전트(하네스)는 실주문을 내지 않는다.** 백테스트·페이퍼 체결·신호 산출까지만 자동화하고, 실거래 주문은 **사람이 직접** 집행한다.
- 저장소에 보관하는 바이낸스 API 키는 **Read-only 스코프만** 허용한다. 거래(Trade)·출금(Withdraw) 권한 키는 `.env`에 두지 않는다.
- 시드는 잃어도 되는 금액으로 한정한다. 커스터디얼 거래소 잔고는 원본 전략의 "소액만" 원칙(`docs/SECURITY.md`)을 그대로 따른다.

---

## 5. 트랙 F — 전략 랩 SaaS

### 5-1. 제품 형태: 백테스트 랩 툴 (확정)

고객이 **자기 전략을 정의해 돌려보는 소프트웨어**를 판매한다. 내 매매 신호를 구독시키는 형태가 아니다.

**이 선택은 규제 회피 설계다.** 한국에서 대가를 받고 불특정 다수에게 매매 판단·신호를 제공하는 행위는 유사투자자문업 신고 대상이 될 수 있다. 도구 판매는 그 경계 밖에 둔다. 따라서 제품 카피에서 지키는 선:

- ✅ "당신의 전략을 검증하는 백테스트 엔진"
- ✅ 내 전략의 과거 성과를 **사례로** 공개 (사실 기술)
- ❌ "이 종목을 지금 사세요" / 개별 매매 신호 실시간 전송
- ❌ 수익 보장·기대 수익률 약속

### 5-2. 구성

- 랩 UI: 전략 파라미터 입력 → 백테스트 실행 → 리포트(자산곡선·지표·트레이드 목록)
- 무료 티어(제한된 기간·심볼) → 유료 구독
- 결제: **B4(라이트닝 결제 페이지)를 그대로 재사용** — 기존 트랙 B 인프라가 여기서 회수된다

---

## 6. 기존 목표와의 관계

**첫 600 sats 수령 목표(`goal`)는 변경하지 않는다.** B1/B2는 여전히 사람 작업으로 남아 있고, 해결되는 즉시 원래 경로가 재개된다. 확장 트랙은 그 블로커와 **독립적으로** 전진해 하네스의 유휴 회차를 없애는 역할이다.

또한 F3(구독 결제)가 B4에 의존하므로, **B1을 해결할 경제적 동기가 오히려 커진다** — 지갑이 없으면 SaaS 매출도 받을 수 없다.

---

## 7. 리스크

**트레이딩**
- 단타는 기대수익이 음수이기 쉽다(수수료·슬리피지·스프레드). 게이트(4-1)를 지키지 않으면 시드는 소멸한다.
- **과최적화(overfitting)**가 가장 흔한 실패 모드다. 인샘플 성과는 홍보 근거로 쓰지 않는다.
- 백테스트↔실전 괴리: 체결 미달, 부분 체결, 거래소 장애. 페이퍼 30일은 이 괴리를 재는 장치다.

**규제·세무**
- 유사투자자문업: §5-1의 경계를 넘지 않는다. 형태를 바꾸려면 사전에 확인이 필요하다.
- 가상자산 소득 과세·SaaS 매출의 사업소득 신고는 원본 전략 §6을 그대로 따른다.
- 공개 성과 표기는 §3-2 규약을 지킨다. 부풀린 수치는 규제 이전에 제품 신뢰의 문제다.

**보안**
- Read-only 키 원칙(§4-2). Trade/Withdraw 키를 저장소·`.env`·로그에 두지 않는다.
- 거래소 계정에 2FA 필수. 커스터디얼 잔고는 소액만.

---

## 8. 결론

자본 0에서 단타를 시작할 수는 없지만, **단타를 검증할 랩은 오늘 만들 수 있다.** 그 랩이 만들어내는 결과물 하나가 트레이딩의 근거이자, 홍보 콘텐츠이자, 판매할 제품이 된다. 실거래는 그 검증을 통과한 뒤에 사람이 집행한다.
